<!DOCTYPE html>
<html>
    <head>
        {% include 'header.html' %}
        <title>Shoe Finder</title>
    </head>
    <body>
        <div class="container">
            {% if stores is not empty %}
                <h2>Here are all of the stores!</h2>
                <ul>
                    {% for store in stores %}
                        <li><a href="/store/{{ store.getId }}">{{ store.getName }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
                <h4>There are no stores added yet! Use the form below to start adding some!</h4>
            {% endif %}

            <div class="row">
                <form class="col s12" action="/add_store" method="post">
                    <div class="row">
                        <div class="input-field col s6">
                            <label for="name">Store Name</label>
                            <input name="name" type="text" class="validate" required>
                        </div>
                    </div>
                    <button class="btn waves-effect waves-light submit-button" type="submit" name="action">Add Store</button>
                </form>
            </div>

            <div class="row">
                <form class="col s12" action="/delete_stores" method="post">
                    <button class="btn waves-effect waves-light delete-button" type="submit">Delete ALL Stores</button>
                </form>
            </div>

            {% include 'footer.html' %}
        </div>
    </body>
</html>
